import{authorizeApi,apiError,isResponse}from"../../../api-helpers";export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){const c=await authorizeApi("idea.review");if(isResponse(c))return c;const{id}=await params,b=await r.json()as{status?:string;notes?:string};if(!["Under Review","Approved","Rejected"].includes(b.status??""))return apiError(422,"INVALID_STATUS","Select a review outcome.",c.correlationId,c.timestamp);const{reviewIdea}=await import("../../../../../../db/ideas");if(!await reviewIdea(id,b.status!,b.notes??"",c.principal.user.userId,c.correlationId))return apiError(409,"IDEA_UNAVAILABLE","Idea is unavailable for review.",c.correlationId,c.timestamp);return Response.json({data:{ok:true}});}
