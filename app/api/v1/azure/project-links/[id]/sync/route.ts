import{authorizeApi,apiError,isResponse}from"../../../../api-helpers";import{AzureDevOpsProvider}from"../../../../../../integrations/azure-devops-provider";export const dynamic="force-dynamic";
export async function POST(_request:Request,{params}:{params:Promise<{id:string}>}){const context=await authorizeApi("integration.sync");if(isResponse(context))return context;const{id}=await params,{getSyncContext,beginSync,completeSync,failSync}=await import("../../../../../../../db/azure-sync"),sync=await getSyncContext(id);if(!sync)return apiError(404,"AZURE_PROJECT_LINK_NOT_FOUND","Azure Project link was not found.",context.correlationId,context.timestamp);const{getCredential}=await import("../../../../../../../db/azure-connections"),credential=getCredential(sync.credentialBinding);if(!credential)return apiError(424,"AZURE_CREDENTIAL_NOT_CONFIGURED","Configure the server credential before synchronizing.",context.correlationId,context.timestamp);const run=await beginSync(id,context.principal.user.userId,context.correlationId);if(run.kind==="in_progress")return apiError(409,"AZURE_SYNC_IN_PROGRESS","This Azure Project link is already synchronizing.",context.correlationId,context.timestamp);try{const provider=new AzureDevOpsProvider({organization:sync.organization,authType:sync.authType as"ENTRA_APPLICATION"|"PAT",credential}),metrics=await provider.getDevelopmentMetrics(sync.azureProjectId,sync.azureTeamId),result=await completeSync(run.id,id,sync.projectId,metrics,context.principal.user.userId,context.correlationId);return Response.json({data:result,meta:{correlationId:context.correlationId,timestamp:result.syncedAt}},{headers:{"cache-control":"no-store"}});}catch(error){const status=(error as{status?:number}).status,code=status===401?"AZURE_AUTHENTICATION_FAILED":status===403?"AZURE_PERMISSION_DENIED":status===429?"AZURE_RATE_LIMITED":error instanceof DOMException&&error.name==="AbortError"?"AZURE_TIMEOUT":"AZURE_SYNC_FAILED",message=code==="AZURE_AUTHENTICATION_FAILED"?"Azure rejected the configured credential.":code==="AZURE_PERMISSION_DENIED"?"The credential cannot read Azure work items.":code==="AZURE_RATE_LIMITED"?"Azure rate-limited synchronization. Try again later.":code==="AZURE_TIMEOUT"?"Azure synchronization timed out.":"Azure progress could not be synchronized.";await failSync(run.id,id,code,message,context.principal.user.userId,context.correlationId);return apiError(status===401?401:status===403?403:status===429?429:502,code,message,context.correlationId,new Date().toISOString());}}
