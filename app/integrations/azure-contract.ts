export const azureAuthTypes = ["ENTRA_APPLICATION", "PAT"] as const;
export const syncFrequencies = ["EVERY_15_MINUTES", "EVERY_30_MINUTES", "HOURLY", "DAILY"] as const;
export type AzureConnectionInput = { name:string;organization:string;authType:(typeof azureAuthTypes)[number];credentialBinding:string;syncFrequency:(typeof syncFrequencies)[number];enabled:boolean };

export function validateAzureConnection(body:unknown){const source=body&&typeof body==="object"?body as Record<string,unknown>:{};const name=typeof source.name==="string"?source.name.trim():"",organization=typeof source.organization==="string"?source.organization.trim().toLowerCase():"",authType=typeof source.authType==="string"?source.authType:"ENTRA_APPLICATION",credentialBinding=typeof source.credentialBinding==="string"?source.credentialBinding.trim().toUpperCase():"",syncFrequency=typeof source.syncFrequency==="string"?source.syncFrequency:"HOURLY",enabled=source.enabled!==false;const details:Record<string,string>={};if(name.length<2||name.length>100)details.name="Name must contain 2–100 characters.";if(!/^[a-z0-9][a-z0-9-]{1,48}[a-z0-9]$/.test(organization))details.organization="Enter the Azure DevOps organization slug, not a URL.";if(!azureAuthTypes.includes(authType as AzureConnectionInput["authType"]))details.authType="Select a supported authentication method.";if(!/^AZURE_DEVOPS_[A-Z0-9_]{3,80}$/.test(credentialBinding))details.credentialBinding="Use a server secret binding beginning AZURE_DEVOPS_.";if(!syncFrequencies.includes(syncFrequency as AzureConnectionInput["syncFrequency"]))details.syncFrequency="Select a supported synchronization frequency.";const value:AzureConnectionInput={name,organization,authType:authType as AzureConnectionInput["authType"],credentialBinding,syncFrequency:syncFrequency as AzureConnectionInput["syncFrequency"],enabled};return Object.keys(details).length?{ok:false as const,details}:{ok:true as const,value};}

export function organizationUrl(organization:string){return `https://dev.azure.com/${organization}`;}
