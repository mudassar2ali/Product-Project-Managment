import type { ConnectionTestResult,DevelopmentMetrics,EngineeringDeliveryProvider } from "./engineering-delivery-provider";
type Config={organization:string;authType:"ENTRA_APPLICATION"|"PAT";credential:string};type AzureList<T>={count:number;value:T[]};
export class AzureDevOpsProvider implements EngineeringDeliveryProvider{constructor(private readonly config:Config){}private authorization(){return this.config.authType==="PAT"?`Basic ${btoa(`:${this.config.credential}`)}`:`Bearer ${this.config.credential}`;}private async call<T>(path:string,init?:RequestInit):Promise<T>{const controller=new AbortController(),timeout=setTimeout(()=>controller.abort(),12000);try{const response=await fetch(`https://dev.azure.com/${encodeURIComponent(this.config.organization)}${path}`,{...init,headers:{accept:"application/json",authorization:this.authorization(),...(init?.headers??{})},signal:controller.signal});if(!response.ok){const error=new Error(`Azure DevOps returned ${response.status}.`) as Error&{status?:number};error.status=response.status;throw error;}return await response.json() as T;}finally{clearTimeout(timeout);}}private request<T>(path:string){return this.call<T>(path);}private query<T>(path:string,body:unknown){return this.call<T>(path,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});}async testConnection():Promise<ConnectionTestResult>{const testedAt=new Date().toISOString();try{const projects=await this.getProjects();return{ok:true,authenticated:true,projectsVisible:projects.length,testedAt,message:`Connection succeeded. ${projects.length} Project${projects.length===1?"":"s"} visible.`};}catch(error){const status=(error as {status?:number}).status,code=status===401?"AZURE_AUTHENTICATION_FAILED":status===403?"AZURE_PERMISSION_DENIED":status===429?"AZURE_RATE_LIMITED":error instanceof DOMException&&error.name==="AbortError"?"AZURE_TIMEOUT":"AZURE_API_FAILURE";return{ok:false,authenticated:false,projectsVisible:0,testedAt,errorCode:code,message:code==="AZURE_AUTHENTICATION_FAILED"?"Azure rejected the configured credential.":code==="AZURE_PERMISSION_DENIED"?"The credential does not have permission to read Azure Projects.":code==="AZURE_RATE_LIMITED"?"Azure rate-limited the request. Try again later.":code==="AZURE_TIMEOUT"?"Azure did not respond before the safe timeout.":"Azure DevOps could not be reached."};}}async getProjects(){const result=await this.request<AzureList<{id:string;name:string;description?:string;state:string;url:string}>>(`/_apis/projects?stateFilter=WellFormed&$top=100&api-version=7.1`);return result.value.map(p=>({id:p.id,name:p.name,description:p.description??null,state:p.state,url:p.url}));}async getTeams(projectId:string){const result=await this.request<AzureList<{id:string;name:string;description?:string;url:string}>>(`/_apis/projects/${encodeURIComponent(projectId)}/teams?$top=100&api-version=7.1`);return result.value.map(team=>({id:team.id,name:team.name,description:team.description??null,url:team.url}));}async getIterations():Promise<unknown[]>{throw new Error("Iteration retrieval is delivered in Step 11.");}async getCurrentSprint():Promise<null>{throw new Error("Sprint retrieval is delivered in Step 11.");}async getWorkItems(projectId:string,ids:number[]){if(!ids.length)return[];const result=await this.request<{value:Array<{id:number;rev:number;fields:Record<string,unknown>}>}>(`/${encodeURIComponent(projectId)}/_apis/wit/workitems?ids=${ids.slice(0,200).join(",")}&fields=System.Id,System.State,System.WorkItemType,System.ChangedDate&api-version=7.1`);return result.value;}async getWorkItem(projectId:string,id:number){return(await this.getWorkItems(projectId,[id]))[0]??null;}async getSprintMetrics():Promise<unknown>{throw new Error("Sprint metrics are delivered in Step 11.");}async getDevelopmentMetrics(projectId:string):Promise<DevelopmentMetrics>{const result=await this.query<{workItems:Array<{id:number}>}>(`/${encodeURIComponent(projectId)}/_apis/wit/wiql?api-version=7.1`,{query:"SELECT [System.Id] FROM WorkItems WHERE [System.TeamProject] = @project ORDER BY [System.ChangedDate] DESC"}),ids=result.workItems.slice(0,1000).map(item=>item.id),items=[] as Array<{id:number;rev:number;fields:Record<string,unknown>}>;for(let i=0;i<ids.length;i+=200)items.push(...await this.getWorkItems(projectId,ids.slice(i,i+200)) as typeof items);const done=new Set(["closed","done","completed","resolved","removed"]),activeStates=new Set(["active","committed","in progress","doing"]);let completed=0,active=0;for(const item of items){const state=String(item.fields["System.State"]??"").toLowerCase();if(done.has(state))completed++;else if(activeStates.has(state))active++;}const total=items.length,other=total-completed-active;return{totalItems:total,completedItems:completed,activeItems:active,otherItems:other,progress:total?Math.round(completed/total*100):0,sourceRevision:items.map(item=>`${item.id}:${item.rev}`).sort().join("|")||"EMPTY"};}}
