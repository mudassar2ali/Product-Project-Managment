import type{ConnectionTestResult,DevelopmentMetrics,EngineeringDeliveryProvider,SprintMetrics}from"./engineering-delivery-provider";type Config={organization:string;authType:"ENTRA_APPLICATION"|"PAT";credential:string};type AzureList<T>={count:number;value:T[]};type Item={id:number;rev:number;fields:Record<string,unknown>};
export class AzureDevOpsProvider implements EngineeringDeliveryProvider{constructor(private readonly config:Config){}private auth(){return this.config.authType==="PAT"?`Basic ${btoa(`:${this.config.credential}`)}`:`Bearer ${this.config.credential}`;}private async call<T>(path:string,init?:RequestInit):Promise<T>{const controller=new AbortController(),timeout=setTimeout(()=>controller.abort(),12000);try{const response=await fetch(`https://dev.azure.com/${encodeURIComponent(this.config.organization)}${path}`,{...init,headers:{accept:"application/json",authorization:this.auth(),...(init?.headers??{})},signal:controller.signal});if(!response.ok){const error=new Error(`Azure DevOps returned ${response.status}.`)as Error&{status?:number};error.status=response.status;throw error;}return await response.json()as T;}finally{clearTimeout(timeout);}}private request<T>(path:string){return this.call<T>(path);}private query<T>(path:string,body:unknown){return this.call<T>(path,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});}async testConnection():Promise<ConnectionTestResult>{const testedAt=new Date().toISOString();try{const projects=await this.getProjects();return{ok:true,authenticated:true,projectsVisible:projects.length,testedAt,message:`Connection succeeded. ${projects.length} Projects visible.`};}catch(error){const status=(error as{status?:number}).status,code=status===401?"AZURE_AUTHENTICATION_FAILED":status===403?"AZURE_PERMISSION_DENIED":status===429?"AZURE_RATE_LIMITED":error instanceof DOMException&&error.name==="AbortError"?"AZURE_TIMEOUT":"AZURE_API_FAILURE";return{ok:false,authenticated:false,projectsVisible:0,testedAt,errorCode:code,message:"Azure DevOps could not be reached."};}}async getProjects(){const r=await this.request<AzureList<{id:string;name:string;description?:string;state:string;url:string}>>(`/_apis/projects?stateFilter=WellFormed&$top=100&api-version=7.1`);return r.value.map(p=>({id:p.id,name:p.name,description:p.description??null,state:p.state,url:p.url}));}async getTeams(projectId:string){const r=await this.request<AzureList<{id:string;name:string;description?:string;url:string}>>(`/_apis/projects/${encodeURIComponent(projectId)}/teams?$top=100&api-version=7.1`);return r.value.map(t=>({id:t.id,name:t.name,description:t.description??null,url:t.url}));}async getIterations(projectId:string,teamId:string){const r=await this.request<{value:Array<{id:string;name:string;path:string;attributes?:{startDate?:string;finishDate?:string}}>}>(`/${encodeURIComponent(projectId)}/${encodeURIComponent(teamId)}/_apis/work/teamsettings/iterations?api-version=7.1`);return r.value.map(i=>({id:i.id,name:i.name,path:i.path,startDate:i.attributes?.startDate??null,finishDate:i.attributes?.finishDate??null}));}async getCurrentSprint(projectId:string,teamId:string){const r=await this.request<{value:Array<{id:string;name:string;path:string;attributes?:{startDate?:string;finishDate?:string}}>}>(`/${encodeURIComponent(projectId)}/${encodeURIComponent(teamId)}/_apis/work/teamsettings/iterations?$timeframe=current&api-version=7.1`),i=r.value[0];return i?{id:i.id,name:i.name,path:i.path,startDate:i.attributes?.startDate??null,finishDate:i.attributes?.finishDate??null}:null;}async getWorkItems(projectId:string,ids:number[]){if(!ids.length)return[];return(await this.request<{value:Item[]}>(`/${encodeURIComponent(projectId)}/_apis/wit/workitems?ids=${ids.slice(0,200).join(",")}&fields=System.Id,System.State,System.WorkItemType,System.ChangedDate&api-version=7.1`)).value;}async getWorkItem(projectId:string,id:number){return(await this.getWorkItems(projectId,[id]))[0]??null;}private async hydrate(projectId:string,ids:number[]){const items:Item[]=[];for(let i=0;i<ids.length;i+=200)items.push(...await this.getWorkItems(projectId,ids.slice(i,i+200))as Item[]);return items;}async getSprintMetrics(projectId:string,teamId:string,iterationId:string):Promise<SprintMetrics>{const iteration=(await this.getIterations(projectId,teamId)).find(i=>i.id===iterationId);if(!iteration)throw new Error("Current Azure iteration was not found.");const r=await this.request<{workItemRelations:Array<{target?:{id:number}}>}>(`/${encodeURIComponent(projectId)}/${encodeURIComponent(teamId)}/_apis/work/teamsettings/iterations/${encodeURIComponent(iterationId)}/workitems?api-version=7.1`),ids=[...new Set(r.workItemRelations.map(x=>x.target?.id).filter((id):id is number=>typeof id==="number"))],items=await this.hydrate(projectId,ids),done=new Set(["closed","done","completed","resolved","removed"]),completed=items.filter(i=>done.has(String(i.fields["System.State"]??"").toLowerCase())).length,total=items.length,progress=total?Math.round(completed/total*100):0,daysRemaining=iteration.finishDate?Math.ceil((new Date(iteration.finishDate).getTime()-Date.now())/86400000):null,elapsed=iteration.startDate&&iteration.finishDate?Math.max(0,Math.min(1,(Date.now()-new Date(iteration.startDate).getTime())/(new Date(iteration.finishDate).getTime()-new Date(iteration.startDate).getTime()))):null,health=daysRemaining!==null&&daysRemaining<0&&progress<100?"OVERDUE":elapsed!==null&&progress+15<elapsed*100?"AT_RISK":"ON_TRACK";return{...iteration,totalItems:total,completedItems:completed,activeItems:total-completed,progress,daysRemaining,health,sourceRevision:items.map(i=>`${i.id}:${i.rev}`).sort().join("|")||"EMPTY"};}async getDevelopmentMetrics(projectId:string):Promise<DevelopmentMetrics>{const r=await this.query<{workItems:Array<{id:number}>}>(`/${encodeURIComponent(projectId)}/_apis/wit/wiql?api-version=7.1`,{query:"SELECT [System.Id] FROM WorkItems WHERE [System.TeamProject] = @project ORDER BY [System.ChangedDate] DESC"}),items=await this.hydrate(projectId,r.workItems.slice(0,1000).map(i=>i.id)),done=new Set(["closed","done","completed","resolved","removed"]),activeStates=new Set(["active","committed","in progress","doing"]),completed=items.filter(i=>done.has(String(i.fields["System.State"]??"").toLowerCase())).length,active=items.filter(i=>activeStates.has(String(i.fields["System.State"]??"").toLowerCase())).length,total=items.length;return{totalItems:total,completedItems:completed,activeItems:active,otherItems:total-completed-active,progress:total?Math.round(completed/total*100):0,sourceRevision:items.map(i=>`${i.id}:${i.rev}`).sort().join("|")||"EMPTY"};}}
