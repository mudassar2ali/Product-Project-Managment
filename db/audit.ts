import{env}from"cloudflare:workers";type Input={q?:string;entity?:string;action?:string;source?:string;from?:string;to?:string;page?:number;pageSize?:number};const sensitive=/token|secret|password|credential|authorization|api.?key|private.?key/i;function redact(value:unknown):unknown{if(Array.isArray(value))return value.map(redact);if(value&&typeof value==='object')return Object.fromEntries(Object.entries(value).map(([k,v])=>[k,sensitive.test(k)?'[REDACTED]':redact(v)]));return value}function parse(value:string|null){if(!value)return null;try{return redact(JSON.parse(value))}catch{return'[UNAVAILABLE]'}}export async function listAudit(input:Input){const where:string[]=["1=1"],bindings:unknown[]=[];if(input.entity){where.push('a.entity_type=?');bindings.push(input.entity)}if(input.action){where.push('a.action=?');bindings.push(input.action)}if(input.source){where.push('a.source=?');bindings.push(input.source)}if(input.from){where.push("date(a.occurred_at)>=date(?)");bindings.push(input.from)}if(input.to){where.push("date(a.occurred_at)<=date(?)");bindings.push(input.to)}if(input.q){where.push("(lower(a.entity_id) LIKE ? OR lower(a.correlation_id) LIKE ? OR lower(COALESCE(u.display_name,'')) LIKE ?)");const q=`%${input.q.toLowerCase()}%`;bindings.push(q,q,q)}const page=Math.max(1,input.page||1),pageSize=Math.min(100,Math.max(1,input.pageSize||25)),filter=where.join(' AND '),[rows,count,facets]=await Promise.all([env.DB.prepare(`SELECT a.id,a.entity_type entityType,a.entity_id entityId,a.action,a.before_json beforeJson,a.after_json afterJson,COALESCE(u.display_name,'System / service') actor,a.source,a.correlation_id correlationId,a.occurred_at occurredAt FROM audit_logs a LEFT JOIN users u ON u.id=a.actor_user_id WHERE ${filter} ORDER BY a.occurred_at DESC,a.id DESC LIMIT ? OFFSET ?`).bind(...bindings,pageSize,(page-1)*pageSize).all<Record<string,unknown>>(),env.DB.prepare(`SELECT COUNT(*) total FROM audit_logs a LEFT JOIN users u ON u.id=a.actor_user_id WHERE ${filter}`).bind(...bindings).first<{total:number}>(),env.DB.prepare("SELECT (SELECT COUNT(*) FROM audit_logs) total,(SELECT COUNT(DISTINCT entity_type) FROM audit_logs) entities,(SELECT COUNT(*) FROM audit_logs WHERE source='AZURE_DEVOPS') integrations,(SELECT COUNT(*) FROM audit_logs WHERE date(occurred_at)=date('now')) today").first<Record<string,number>>()]);return{rows:rows.results.map(r=>({...r,before:parse(r.beforeJson as string|null),after:parse(r.afterJson as string|null),beforeJson:undefined,afterJson:undefined})),total:count?.total||0,summary:facets||{total:0,entities:0,integrations:0,today:0},parameters:{...input,page,pageSize},generatedAt:new Date().toISOString(),retention:"Stage 1 audit events are retained for the life of the environment; formal enterprise retention policy is pending governance approval."}}
