import test from'node:test';import assert from'node:assert/strict';import{readFile,readdir}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');
test('global errors are safe recoverable and correlated',async()=>{const [e,n,a]=await Promise.all([read('app/error.tsx'),read('app/not-found.tsx'),read('app/api/v1/api-helpers.ts')]);assert.match(e,/Try again/);assert.match(e,/digest/);assert.doesNotMatch(e,/error\.message/);assert.match(n,/Return to Dashboard/);assert.match(a,/correlationId/)});
test('production responses declare the Stage 1 security baseline',async()=>{const s=await read('proxy.ts');for(const x of['Content-Security-Policy','frame-ancestors','X-Frame-Options','X-Content-Type-Options','Referrer-Policy','Permissions-Policy'])assert.match(s,new RegExp(x));assert.match(s,/dev\.azure\.com/)});
test('accepted modules are exposed while unauthorized future modules remain absent',async()=>{const s=await read('app/command-center-shell.tsx');for(const x of['Dashboard','Products','Projects','Milestones','Ideas','RAID','Reports','Integrations','Audit Trail','BRD / PRD','Requirements'])assert.match(s,new RegExp(`label: "${x.replaceAll('/','\\/')}"`));for(const x of['TAM','SAM','SOM','Scorecards','Market Intelligence'])assert.doesNotMatch(s,new RegExp(`label: "${x}"`))});
test('critical domain APIs and acceptance suites are present',async()=>{const files=await readdir(new URL('../tests/',import.meta.url));for(const x of['product-management.test.mjs','project-management.test.mjs','azure-progress-sync.test.mjs','idea-management.test.mjs','basic-reports.test.mjs','audit-logging.test.mjs'])assert.ok(files.includes(x));const routes=['app/api/v1/products/route.ts','app/api/v1/projects/route.ts','app/api/v1/dashboard/route.ts','app/api/v1/reports/[key]/route.ts','app/api/v1/audit/route.ts'];for(const r of routes)assert.ok((await read(r)).length>0)});
